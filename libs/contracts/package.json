{
  "name": "@libs/contracts",
  "version": "0.0.1",
  "description": "Shared enums, events, and DTOs — the canonical domain contract layer",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  }
}
